import React from 'react'

const App = () => {
  let email=import.meta.env.EMAIL;
      console.log(ema);
  let pass=import.meta.env.VITE_PASS;
      console.log(pass);
  return (
    <div>
      
    </div>
  )
}

export default App